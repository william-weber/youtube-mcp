{
  "name": "yt-playlist-mcp",
  "version": "0.1.0",
  "description": "Local MCP server for YouTube search and playlist creation",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "auth": "npm run build && node dist/auth-cli.js",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.15.0",
    "googleapis": "^150.0.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "typescript": "^5.8.0"
  }
}
